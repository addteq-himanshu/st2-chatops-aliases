---
name: "service_status"
runner_type: "run-local"
sudo: true
description: "Check service status"
enabled: true
parameters:
    service:
        type: "string"
        description: "Service Name"
        required: true
        position: 0
cmd:
      immutable: true
      default: "service {{service}} status"
