---
name: "deploy"
description: "Deploy a release to given environment"
action_ref: "st2-chatops-aliases.deploy"
formats:
  - "deploy {{environment}} {{release}}"
result:
  format: "```{{ execution.result.stdout }}```"
